server:
  port: ${SERVER_PORT}
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          auth: true
          starttls:
            enable: true
        debug: false
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
